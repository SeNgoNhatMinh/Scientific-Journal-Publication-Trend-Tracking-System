# Railway — Web Service (Root Directory = backend)
# https://docs.railway.com/guides/config-as-code

[build]
builder = "NIXPACKS"

[deploy]
startCommand = "npm start"
healthcheckPath = "/health"
healthcheckTimeout = 120
restartPolicyType = "ON_FAILURE"
