# SciTrend Web Frontend Environment Configuration

# Set to 'true' to use the local development backend (default).
# Set to 'false' to proxy the production Railway URL directly.
VITE_USE_LOCAL_BACKEND=true

# Optional: Override the local backend target URL
# VITE_BACKEND_URL=http://localhost:5000
