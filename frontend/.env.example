# SciTrend Web Frontend — cấu hình local (copy thành .env)

# Luôn dùng backend chạy trên máy (mặc định)
VITE_USE_LOCAL_BACKEND=true

# URL backend local (npm start trong thư mục backend)
VITE_BACKEND_URL=http://localhost:5000
